requests>=2.31.0
urllib3>=2.0.0
aiohttp>=3.10.0
scikit-learn==1.8.0
joblib>=1.4.0
scipy>=1.11.0
python-dotenv>=1.0.0
pytest>=8.0.0
playwright>=1.54.0
# v82: TLS-fingerprint (Cloudflare) rescue transport — optional at runtime
# (code guards ImportError), required in CI where the network is fast.
curl-cffi>=0.11.0
